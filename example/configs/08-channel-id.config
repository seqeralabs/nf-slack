/*
 * EXAMPLE 8: Specific Channel ID
 *
 * This example demonstrates how to send notifications to a specific channel
 * by ID instead of name.
 *
 * Features enabled:
 * - Notifications sent to a channel specified by ID
 * - Default message templates
 */

plugins {
    id 'nf-slack@0.4.0'
}

slack {
    bot {
        token = System.getenv('SLACK_BOT_TOKEN')
        // You can use a channel ID (e.g. 'C052XN379RA') or a channel name
        channel = 'C052XN379RA'
    }
}
